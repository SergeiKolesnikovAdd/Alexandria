import { Left } from "components";
import { ButtonWrapper } from "./button-slider-left.style";

export const ButtonSliderLeft = ({ ...props }) => (
  <ButtonWrapper {...props}>
    <Left/>
  </ButtonWrapper>
);
