import { Right } from "components";
import { ButtonWrapper } from "./button-news.style";

export const ButtonNews = ({ ...props }) => (
  <ButtonWrapper {...props}>
    <Right/>
  </ButtonWrapper>
);
